/**
 * App branding.
 *
 * Only user-facing display strings live here. The upstream project name still
 * appears in places that are functional rather than cosmetic — the `freeSleep`
 * field on the device-status schema, the /home/dac/free-sleep install path on
 * the Pod, and the upstream GitHub URLs used for version checks — and those
 * must not be renamed.
 */
export const APP_NAME = 'GBedOS';
export const APP_TAGLINE = 'by Geo';
export const APP_DESCRIPTION = 'Bed climate control';
